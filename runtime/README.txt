Bundled Node.js runtime
=======================

node.exe is included so the Teambuilder Roster Import Tool can run on Windows
without requiring the user to install Node.js.

The launcher in the project root invokes this executable directly. Do not move
node.exe out of this folder.

Node.js is distributed under its own license. See:
https://github.com/nodejs/node/blob/main/LICENSE
